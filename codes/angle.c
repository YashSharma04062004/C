#include<stdio.h>
void main(){
    int a,b,c,d;
    printf("enter angles of triangle : \n");
    scanf("%d %d %d",&a,&b,&c);
    d=a+b+c;
    if(d==180){
        printf("these are angle of triangle");
    }
    else{66
        printf("these are not angles of triangle");
    }
}